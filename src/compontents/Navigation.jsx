
const Navigation = ({idName}) => {
  return (
    <nav id={idName}>
            <a href="#Home">Home</a>
            <a href="#About">About</a>
            <a href="#Contact">Contact</a>
    </nav>
  )
}
export default Navigation